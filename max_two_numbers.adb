with Ada.Text_IO;
use Ada.Text_IO;

procedure Max_two_numbers is
 a:Integer := 30;
   b:Integer := 20;
begin
   if a<b then
      Put_Line("B is the greater integer");
               else
                  Put_Line("A is the greater integer");
               end if;

end Max_two_numbers;
